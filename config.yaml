bids_root: /path/to/bids_root
bids_cache: /path/to/bids_config.json
tabular: /path/to/tabular.csv
template: /path/to/template.nii.gz
mask: /path/to/mask.nii.gz

analysis:
  query: ""
  smoothing_fwhm: 2.0
  voxel_size: 4.0
  permutations: 200
  random_seed: 42
  multiple_comparisons: FDR

environment:
  testing: true

notes:
  no_raw_blsa_exported: true
  user_note: ""